You are implementing a single story from the development tracker at index.html. That file contains an embedded JSON block (<script id="tracker-data" type="application/json">) that is the single source of truth for all tickets.
The story to implement is: STORY-X.Y (replace with the actual ID).
Process:

Read the ticket. Parse #tracker-data, find STORY-X.Y, and read its description, requirements, and acceptance_criteria. If you need the original requirement wording, read only the relevant @docs/ section it cites. Do not scan the wider codebase beyond what implementing this story needs.
Implement the story. Write the code that satisfies every acceptance criterion. Follow the existing conventions of the codebase in the files you touch.
Write a test for each acceptance criterion. For every entry in acceptance_criteria, write an automated test that proves that specific criterion. The test should fail before your implementation and pass after. Name tests so they're traceable to the criterion (e.g. test_STORY_X_Y_AC1_*).
Run the tests and confirm they pass. If a criterion genuinely cannot be expressed as an automated test, leave its test as null and explain why in the writeback (step 5).
Write back the pointers into STORY-X.Y in index.html, modifying only this story's object:

For each acceptance criterion, set its test field to the identifier of the test that proves it (e.g. tests/auth_test.py::test_STORY_2_1_AC1_login_success), or leave null if step 4 found none.
Set verify.files to the actual source paths you created or changed for this story (real paths now — replace any (inferred) guesses).
Set verify.tests to the test identifiers you wrote.
Set verify.command to a command that runs exactly this story's tests with machine-readable output (e.g. pytest tests/auth_test.py -k STORY_2_1 --json).
Set status to DONE if all criteria have passing tests; IN_REVIEW if implemented but any criterion relies on a null test; IN_PROGRESS if you could not finish.
Set last_updated to today's date (ISO 8601).
Write a one-line audit_notes summary of what was implemented and the test outcome.


Constraints:

Modify only the STORY-X.Y object in the JSON. Do not touch other tickets, the verify/acceptance_criteria structure itself, or any HTML/JS outside #tracker-data.
Within STORY-X.Y, do not reword title, description, requirements, or the statement of any acceptance criterion. You are filling in test, verify, status, last_updated, and audit_notes only.
Keep the JSON schema, key ordering, and formatting intact.

Output:

Apply the code, tests, and the JSON writeback.
Print to chat: the files created/changed, the tests written and their pass/fail result, and the story's old → new status.